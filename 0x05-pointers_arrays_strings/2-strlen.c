#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - determine the length of a string
 * @s: te be added
 * Return: an integer
 */
 
int _strlen(char *s)
{
    return(strlen(s));
}
