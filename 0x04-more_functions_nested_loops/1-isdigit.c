#include "main.h"

/**
* _isdigit - checks if the input is a digit
* @c: intger to be used
* Return: 1 if is digit and 0 if is not
*/
int _isdigit(int c)
{
if (c >= 0 && c <= 9)
{
return (1);
}
return (0);
}
